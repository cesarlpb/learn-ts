# Tipos de Errores:
```js
    // Sintaxis:
    console.gol()
    // Tipos:
    let num3: number = '1';
    // Lógica:
    let division: number = 2 / 0;
    // bugs que no son sintaxis ni lógica:
    let mensaje: string;
    switch(num3){
        case 1:
            mensaje = "algo"
            // break
        case 2: 
            mensaje = "algo 2"
        default:
            mensaje = ""
    }
```